{# se introduc rutele care se folosesc in ajax (apiChangeLanguage inseamna numele controller-ului) #}

<script>
	var anafApiRoute = "{{ absolute_url(path('apiAnafValidation'))|escape('js') }}";
var languageApiRoute = "{{ absolute_url(path('apiChangeLanguage'))|escape('js') }}";
</script>
